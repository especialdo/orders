@page "/cities/edit/{CityId:int}"

<h3>Editar ciudad</h3>

@if (city is null)
{
    <Loading />
}
else
{
    <FormWithName @ref="cityForm" Label="Estado/Departamento" Model="city" OnValidSubmit="SaveAsync" ReturnAction="Return" />
}
